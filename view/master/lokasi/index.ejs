<h4 class="text-capitalize">Master Lokasi Cabang</h4>
<div class="row mt-4">
    <div class="col-12">
        <div class="row">
            <div class="col-lg-6 text-end">
                <a href="/master/lokasi/tambah" class="btn btn-sm btn-primary">
                    <i class="fa fa-fw fa-plus-circle"></i> Lokasi
                </a>
            </div>
        </div>
    </div>
    <br>
    <div class="col-12 pt-3">
        <div class="row">
            <div class="col-lg-12">
                <table id="myTable" class="table table-hover">
                    <thead>
                        <tr>
                            <th>No</th>
                            <th>Kode Lokasi</th>
                            <th>Nama Lokasi</th>
                            <th>Description</th>
                            <th>option</th>
                            <th>Delete</th>
                            <th>Created By</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% for( let i=0; i < lokasi.length; i++) { %>
                            <tr>
                                <td><%= i + 1 %></td>
                                <td><%= lokasi[i].kode %></td>
                                <td><%= lokasi[i].nama_lokasi %></td>
                                <td><%= lokasi[i].description %></td>
                                <td>
                                    <a href="/master/lokasi/edit/<%= lokasi[i].id %>" class="btn btn-sm btn-outline-warning">
                                        <i class="fa fa-fw fa-solid fa-edit"></i>
                                    </a>
                                </td>
                                <td>
                                    <form action="/master/lokasi/delete/<%= lokasi[i].id %>" method="post">
                                        <button type="submit" class="btn btn-sm btn-outline-danger"
                                        onclick="return confirm('apakah anda yakin ingin hapus <%= lokasi[i].nama_lokasi %>?')">
                                            <i class="fa fa-fw fa-solid fa-trash"></i>
                                        </button>
                                    </form>
                                </td>
                                
                                <td><%= lokasi[i].user.username  %></td>
                            </tr>
                        <% } %>
                    </tbody>
                    
                </table>
            </div>
        </div>
    </div>
</div>
